<h1>Doctor Page</h1>
<%= @doctor.name %>'s Appointments
<ul>
    <% @doctor.appointments.each do |app| %>
        <li><%= app.time %> - <%= link_to app.patient.name, patient_path(app.patient) %></li>
        <% end %>
</ul>